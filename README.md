# Portfolio

A responsive personal portfolio website built as a standalone HTML page.

## Run Locally

Open `PORTFOLIO.HTML` directly in a browser, or serve the repository with any static web server:

```bash
python -m http.server
```

Then visit `http://localhost:8000/PORTFOLIO.HTML`.